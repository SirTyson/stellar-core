# Failed Investigations: Crypto Subsystem

Condensed failure summaries for investigations targeting the crypto subsystem (signature verification, hashing, XDR serialization, and Soroban host storage key conversion paths). Last updated 2026-05-02.

## Summary Table

| File | Hypothesis | Why Failed | Stage | Key Lesson |
|------|-----------|------------|-------|------------|
| 001-cache-storage-key-conversions.md | Cache storage-key `Val`→`ScVal` conversions within a host invocation | Below threshold — removing all in-apply `Val`→`ScVal` time would save less than 1% of apply time | reviewer | Val/ScVal conversions are not a dominant hotspot; the conversion overhead is tiny relative to host-object visit and ordered-map costs |
| 001-reuse-encoded-contract-events.md | Reuse encoded contract events instead of XDR-encoding them twice | False path — the claimed duplicate serialization pass (`invocation_metering.rs`) is test/SDK-style machinery not on the production soroswap apply path | reviewer | Profile soroswap specifically; instrumentation code in non-production codepaths does not appear in apply-time traces |
| 001-stream-transaction-contents-hash.md | Stream transaction contents hash without allocating XDR opaque buffers | Not on the measured critical path — contents hashes are precomputed and cached during tx-set preparation, outside the `closeLedger` apply window | reviewer | Transaction hash computation happens before apply; savings in that phase do not affect apply-time metrics |
| 001-verify-sig-mostly-outside-applyledger.md | Optimize signature verification cache for soroswap apply | Out of scope — the apparent hotspot is mostly outside `applyLedger`; the objective excludes tx-set construction and validation work | hypothesis | Verify that profiled symbols fall within the `closeLedger` apply window; aggregate process-level hotspots often include pre-apply phases |
| 002-memoize-host-storage-map-lookups.md | Memoize repeated host storage map lookups within a soroswap invocation | Below threshold and wrong target — `Storage::try_get_full_helper` is an XDR ledger-key ordered-map, not the host-object-visit path emphasized by the hypothesis | reviewer | Distinguish host object visits from storage map lookups; the dominant cost in the soroban storage path is host-object budget charging, not the map probe itself |
| 002-single-pass-try-get-contract-data.md | Make `try_get_contract_data` use one storage lookup instead of has-then-get | Below threshold — the redundant `has_contract_data` half of successful calls is too small to reach the 3% Medium floor | reviewer | Double-probe micro-optimizations in Soroban host storage are real but sub-1%; the double-probe is also intentional for metering semantics in p26 |
| 002-specialize-soroban-subseed-hash.md | Specialize Soroban PRNG sub-seed hashing to avoid per-tx XDR allocation | Below threshold — removes a real but tiny allocation in `subSha256`; savings are sub-1% even in the optimistic scenario | reviewer | PRNG seeding overhead is negligible relative to Soroban host execution; micro-allocations in infrequent cryptographic paths cannot reach Medium severity |
| 003-cache-ttl-key-in-inmemory-value-entry.md | Cache TTL key hash in `InMemorySorobanState` `ValueEntry` to skip per-lookup SHA256 recomputation | Below threshold — total in-apply SHA256 time is ~4.17 ms per soroswap ledger out of 621 ms median (~0.67%) | hypothesis | SHA256 for TTL key derivation is a tiny fraction of apply time; caching it cannot reach the 3% Medium floor regardless of implementation quality |
| 004-precompute-footprint-ttl-keys.md | Precompute per-tx footprint TTL keys to deduplicate cross-phase `getTTLKey()` calls | Below threshold — the complete in-apply SHA256 budget for soroswap is ~0.67% of apply time; deduplication recovers only a fraction of that | hypothesis | Cross-phase TTL key recomputation is real but sub-1%; caching at the tx level is blocked by the same SHA256 budget ceiling as H003 |
| 005-cached-contents-hash-apply-accessors.md | Reuse threaded `envelopeContentsHash` in `commonPreApply`/`commonParallelPreApplyReadOnly` instead of calling `getContentsHash()` again | Below threshold — all apply-path `getContentsHash()` calls are cache hits; removing accessor/profiler overhead cannot reach the 1% Low floor | hypothesis | Distinguish cold SHA256 computation from cache-hit accessor calls; cached hash accessors alone are not a Medium-tier soroswap apply bottleneck |
| 006-rust-bridge-small-cxxbufs.md | Borrow small bridge input buffers (`resources`, PRNG seed) instead of passing by value to reduce per-invocation allocations | Below threshold — bridge wrapper zones total only ~22ms across the whole trace; dominant costs lie in host execution and metered XDR serialization | hypothesis | For bridge hypotheses, separate API-shape cleanliness from apply-time impact; small-buffer borrowing is sub-threshold unless bridge wrapper work appears on the critical path at much larger scale |
| 007-cache-ledgerkey-siphash-on-prefetch.md + 011-shorthash-gkey-mutex-lock-free.md | Cache per-key SipHash across `InMemoryBucketState::scan` levels; or make `shortHash::gKey` reads lock-free to eliminate mutex per hash call | Below threshold — the dominant scan cost (~2.1µs mean) is `unordered_set` bucket traversal and XDR equality compare, not SipHash; removing the mutex entirely yields ~5ms aggregate wall time across the run, far below 1% | hypothesis | In-memory bucket scan cost is dominated by index walk and equality compare, not hash computation or mutex acquisition; crypto-only fixes to the scan hash path cannot reach Medium severity |
| 008-shard-or-bypass-verifysig-cache-in-apply.md + 009-shorten-verifysig-cache-key-no-message.md | Bypass `gVerifySigCache` probe for apply-path re-verifications (sticky flag); or shorten cache key to hash only pubkey+signature (not message bytes) | Apply-path share is bounded by `processSignatures` + `checkAllTransactionSignatures` = ~46ms across 65 ledgers = ~0.7ms/ledger = well under 0.2% of apply; even full elimination cannot reach the 1% Low floor | hypothesis | The apply-path verifySig share is structurally capped below 1%; no narrower micro-optimization within that zone can be promoted; future signature hypotheses must find a caller not already bounded by this ceiling |
| 010-zero-copy-modified-ledger-entries-ffi.md | Zero-copy or batched `modified_ledger_entries` FFI return to eliminate per-entry allocation chain | Below threshold — `recordStorageChanges` (C++ XDR-decode) is 28ms self; cxx wrapper overhead ~22ms (per H006); together ~50ms, but realistic savings are a fraction since host-side encoding cannot be eliminated | hypothesis | FFI output redesigns are bounded by soroban-env metered XDR contract; the host must serialize entries for its own accounting before returning them; eliminating C++-side decode does not remove the Rust-side encoding work |
| 012-sha256-one-shot-vs-streaming-class.md | Replace `SHA256` streaming class with one-shot `::SHA256()` for short-input apply-path callers (`getTTLKey`, `subSha256`) | Below threshold — the entire in-apply SHA256 budget (~4.17ms/ledger = ~0.7%) is below the 1% Low floor; Tracy `ZoneScoped` inflates apparent per-call overhead in the trace | hypothesis | Tracy ZoneScoped overhead inflates fine-grained crypto-primitive self-times; projected savings from class-form elimination must be sized against the SHA256 budget ceiling (already sub-1%), not against Tracy self-time |

| 001-remove-module-cache-read-lock-from-apply.md | Remove shared module-cache read lock from apply-time VM instantiation | The claimed inefficiency exists, but the projected gain is below the optimize-soroswap Medium severity threshold. On the cited trace, a 3% apply-time win over 5.774 s requires about 173 ms total savings; across 10,061 cache-hit lookups, that means the lock-free design would need to remove roughly 17 microseconds from every lookup on the apply criti | reviewer | Do not promote a structural shared-lock hypothesis to Medium severity without measuring lock wait or isolating the locked critical section. A mutex in a parallel apply path is worth investigating, but when the critical section is a tiny read-only map lookup and the workload performs only thousands of lookups per second, the severity projection must |
| 001-stack-allocate-vm-call-args.md | Stack-allocate VM call argument buffers | The claimed heap allocation is real, but it is not large enough to clear the optimize-soroswap severity floor. The objective requires at least a Medium projection (3-10% apply-time reduction), which is about 173 ms on the cited 5.774 s `applyLedger` envelope. | reviewer | Do not size host-to-VM marshalling optimizations against an entire Tracy self-time block unless the removable work dominates that block. For `Vm::invoke_function_raw`, the temporary `Vec<wasmi::Value>` is a clean local inefficiency, but metering and relative-object conversion are the real semantic work on the path, and the remaining allocation is t |
| 002-fuse-linear-memory-val-imports.md | Fuse linear-memory `Val` imports for map and vector constructors | The inefficiency exists, but the projected apply-time improvement is below this objective's Medium severity threshold. The cited aggregate constructor self-time is about 184 ms, only slightly above the 3% floor on the 5.774 s `applyLedger` envelope, so a viable Medium hypothesis would need to remove nearly the entire measured constructor self-time. | reviewer | Do not promote linear-memory import cleanups by comparing them to an entire constructor self-time block when the block is barely above the Medium threshold. For these constructors, the local allocation/pass cleanup is real and may be a tidy low-level optimization, but the semantic work that remains dominates enough that it cannot be justified as a  |
| 002-share-initial-storage-map-snapshot.md | Share the initial Soroban storage map snapshot instead of deep-cloning it | The optimization claim overstates the removable work. `storage_map.metered_clone` does allocate a second vector and copy all map pairs, but the map values are `Rc<LedgerKey>` and `Option<(Rc<LedgerEntry>, Option<u32>)>`, and `MeteredClone for Rc<T>` is explicitly shallow. | reviewer | For Soroban storage-map hypotheses, distinguish shallow `Rc`/`Vec` snapshot cloning from the much more expensive functional map rebuilds caused by `MeteredOrdMap::insert`. A viable Medium-tier storage refactor needs isolated measurements for the rebuild/write path, not attribution of all `new map` or `invoke_host_function` self-time to the initial  |
| 013-soroban-host-sha256-syscall-dispatch.md | Optimize Soroban host `compute_hash_sha256` syscall dispatch overhead | In-apply Soroban SHA256 work totals ~1.14% of applyLedger time even before considering that the actual hash work and metering charges cannot be removed. The realistic optimization surface (dispatch zone + scbytes_from_vec allocation) is a fraction of that 1.14% and is well below the 3% Medium severity floor required by this objective. | hypothesis | Soroban host-syscall dispatch overhead for cryptographic primitives is bounded by the per-call invocation count from the contract workload. Soroswap's per-swap SHA256 syscall count is too low to make this surface reach Medium severity. |
| 014-cache-contract-data-ledgerkey-hash.md | Specialize `std::hash<LedgerKey>` for CONTRACT_DATA to skip per-lookup XDR SipHash | The dominant per-lookup cost in `unordered_set<LedgerKey>` containers is XDR `operator==` over the SCVal key, which has identical structural walking complexity to the SipHash. Eliminating the hash side does not eliminate the equality side. | hypothesis | Hash-caching schemes for variable-length XDR keys must be justified by both hash cost AND equality-compare cost — caching only the hash side leaves the equality work intact and yields at most ~50% of the per-probe crypto cost, which is already structurally sub-1% per Meta-Pattern 6. Future LedgerKey-container hypotheses should target restructuring  |
| 015-tx-result-set-xdrsha256.md | Stream-hash `txResultSet` per-result instead of full-set `xdrSha256` at end of apply | The bulk SHA256 over the result set is approximately 1 ms per ledger (~0.3% of apply), structurally below the objective's 1% Low floor and the 3% Medium minimum. The current `xdrSha256` path is already the optimal streaming form. | hypothesis | Per-ledger bulk-XDR SHA256 operations on apply-path output (result sets, header hashes) do not reach Medium severity individually. The total in-apply SHA256 budget remains capped at ~1% even when all such bulk-hash sites are summed. |
| 016-rust-bridge-base64-conversion.md | Eliminate base64 conversions on the apply-path Rust bridge | base64 conversion is not invoked from any `closeLedger` descendant. It is exclusively used by HTTP/CLI command handlers for XDR-text interop, which runs outside the benchmark's measured window. | hypothesis | Before proposing a Rust-bridge optimization, verify the target function is reachable from `closeLedger` in production. The Rust subsystem contains many utility functions (base64, i128 arithmetic, quorum checker, log shim) that are NOT on the apply path. |
| 017-meta-utils-sort-changes-xdrsha256.md | Eliminate per-LedgerEntryChange `xdrSha256` in `MetaUtils::sortChanges` | `normalizeMeta` is **not called from the production apply path**. A repo-wide search (`grep -rn "normalizeMeta" src/`) finds only one caller: `src/ledger/test/LedgerCloseMetaStreamTests.cpp:531`. | hypothesis | Before writing a hypothesis against a "looks expensive" comparator or helper in `src/util/`, verify it is actually invoked from a `closeLedger` descendant. The `MetaUtils.cpp` `sortChanges`/`xdrSha256` pair is one such helper that exists only for test-side meta canonicalization and is not part of the production apply path. |
| 018-xdrhasher-one-shot-serialize.md | Replace `XDRHasher`'s buffered streaming archive with one-shot serialize+hash for short XDR objects on the apply path | Two compounding problems make this sub-threshold: 1. **Tracy ZoneScoped overhead inflates the per-`add` self-time**. | hypothesis | When a crypto-primitive `add` zone shows millions of events in a Tracy trace, treat the headline self-time as Tracy overhead rather than real work — size proposed savings against the **non-Tracy** SHA/BLAKE2 budget ceiling (~0.67% combined for soroswap apply), not against the inflated trace number. Per-element archiver redesigns hit the same SHA256 |
| 019-getfullhash-apply-share.md | Eliminate `getFullHash` recomputation on the apply path | All non-test, non-error-log callers of `getFullHash()` reachable from `applyLedger` are inside exception `catch` blocks; zero cost on the soroswap success path; the 3.33M Tracy calls are owned by `TxSetUtils`/`TxSetFrame`/`TransactionQueue` (tx-set construction, outside apply scope) | hypothesis | Enumerate apply-path callers with grep before sizing savings; `getFullHash` fires only in error handlers and herder-side tx-set work, not on the success apply path — distinguish error-logging uses from production-path uses |
| 020-ledgerheader-prev-final-hash.md | Replace per-ledger `xdrSha256(LedgerHeader)` with incremental hashing | ~130 SHA256 finalisations across the 65-ledger soroswap run ≈ 130µs total ≈ 0.0007% of apply time; bounded by fixed-size payload (~200 bytes) and two calls per ledger; four orders of magnitude below the 3% Medium floor | hypothesis | Fixed-cost-per-ledger SHA256 work (header hashes, txSetResultHash, contract-id preimage) falls inside the 0.67% SHA256 ceiling at single-digit-µs-per-ledger scale; reject up-front unless targeting per-tx or per-entry call sites |
| 021-per-thread-verifysig-cache-already-sharded.md | Per-thread `gVerifySigCache` to remove mutex contention during parallel apply | Cache is already sharded 16 ways (`gVerifySigCacheShards`) with independent per-shard mutexes; expected contention ~0.5 collisions/access with 8 workers; apply-path verifySig ceiling <0.2% even with perfect contention elimination | hypothesis | Read `gVerifySigCacheShards` definition first — the cache is already sharded 16 ways; per-thread caches would lose cross-thread reuse without removing meaningful contention; apply-path verifySig ceiling bounds all sub-optimizations |
| 022-cache-blake2-probe-on-txframe.md | Cache `verifySigCacheKey` BLAKE2 result on `TransactionFrame` to skip per-apply recompute | Apply-path BLAKE2 work is bounded above by the verifySig apply-path ceiling (~0.2% = ~10ms total under generous assumptions); dominant BLAKE2 callers are out-of-scope overlay flood/peer/tracker callsites and CLI `BucketManager::visitLedgerEntries` paths | hypothesis | Apply-path BLAKE2 cannot exceed verifySig apply-path ceiling (~0.2%); verify callsite is reachable from `closeLedger` before sizing — 45ms headline `BLAKE2::add` self-time is dominated by out-of-scope paths |
## Meta-Patterns

1. **SHA256 / Hashing Budget Ceiling**: The entire in-apply SHA256 budget for soroswap is ~4 ms per ledger (~0.67% of apply). Any hypothesis that targets only SHA256 recomputation or TTL key derivation is structurally capped below the 1% Low floor, let alone the 3% Medium floor. Do not write SHA256-only hypotheses for this objective.

2. **Signature Verification Scope**: Signature verification and contents-hash computation happen during tx-set preparation, not during `closeLedger` apply. These are explicitly excluded from the optimize-soroswap objective scope. Profile traces must show the target zone as a descendant of `applyLedger` before writing a hypothesis.

3. **Host-Object Visit vs. Storage Map**: The dominant Soroban storage cost is `VisitObject` budget charging (host object table indexed by 32-bit handle), not the `MeteredOrdMap` binary search over XDR ledger keys. Hypotheses that target the map-probe side will under-estimate the true hotspot and fail the severity floor.

4. **Test/SDK Paths vs. Production Paths**: Some XDR serialization and event-size encoding paths exist only in test, SDK-style, or preflight instrumentation code. Confirm that the target code is reachable during production `closeLedger` before attributing apply time to it.

5. **Apply-Path verifySig and BLAKE2 Share Is Below 1%**: The apply-path share of `verifySig` work is bounded by `processSignatures` + `checkAllTransactionSignatures` = ~46ms across the full 65-ledger soroswap run (~0.7ms/ledger = <0.2% of apply median). Even bypassing the cache probe entirely cannot reach the 1% Low floor. No narrower micro-optimization within the apply-path verifySig zone can be promoted. This ceiling also bounds the BLAKE2 cache-probe prelude inside each verifySig call: apply-path BLAKE2 cannot exceed ~10ms total, and the headline `BLAKE2::add` self-time is dominated by out-of-scope overlay flood/peer/tracker callsites. The cache is already sharded 16 ways (H021). H022 (BLAKE2 cache-key memoization on `TransactionFrame`) is also bounded here. Future verifySig or BLAKE2 sub-optimizations on the apply path must cite a specific callsite that falls outside this ceiling.

6. **In-Memory Bucket Scan Cost Is Index Walk, Not Hash**: `InMemoryBucketState::scan` self-time (~2.1µs mean) is dominated by `unordered_set` bucket traversal and XDR equality compare, not by SipHash computation or `gKeyMutex` acquisition. Eliminating hashing or locking returns a small fraction of scan cost and cannot reach Medium severity; genuine wins require restructuring the index data structure.

7. **Tracy ZoneScoped Overhead Inflates Crypto Primitive Costs**: Fine-grained crypto primitives (SHA256::add, BLAKE2::add, XDRShortHasher constructor) accumulate significant Tracy zone entry/exit overhead that does not exist in non-Tracy production builds. Always size proposed savings against non-Tracy benchmark numbers, not against Tracy self-times.

8. **FFI Bridge Per-Entry Overhead Is Bounded at ~50ms Total**: Bridge input overhead (~22ms across the trace) and C++-side output decode overhead (~28ms) together total ~50ms. The Rust-side encoding work is required by soroban-env metering and cannot be skipped; realistic savings from restructuring the bridge are a small fraction of the output-decode share.
